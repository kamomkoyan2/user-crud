import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
  header: {
    background: '#203040',
  },
  tabs: {
    color: '#FFFFFF',
    marginRight: 20,
    textDecoration: 'none',
    fontSize: 20,
  },
});

export default useStyle;
